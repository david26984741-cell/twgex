# 方法論與限制

這份文件是程式碼裡註解引用的來源（`engine.py` 會標「見 METHODOLOGY.md 第 N 節」）。
網頁上的「怎麼算的」摺疊區是同一套內容的精簡版。

---

## 1. 「造市商曝險」是假設，不是觀測

期交所每日公布的是**全市場**逐履約價的未沖銷契約數。它沒有任何欄位告訴你：

- 哪些部位屬於造市商
- 造市商在該履約價是買方還是賣方

所以任何叫做「造市商 GEX」的東西，本質上都是在某個**方向假設**下算出來的推估值。
本專案用的預設假設是業界最常見的那一種：

| 假設代號 | 買權 | 賣權 | 用在哪 |
|---|---|---|---|
| `net`（買權多・賣權空） | +1 | −1 | GEX 與情境曲線的預設 |
| `gross`（兩邊皆空） | −1 | −1 | VEX 的預設 |

`net` 的直覺是「散戶買賣權避險、造市商接下來；買權則相反」。這個假設會讓
GEX 在現價上方為正、下方為負，於是產生 Gamma Flip 這個看起來很有意義的數字。
**但它的真實性沒有任何資料可以驗證。**

誠實的用法：把這張圖當成**全市場 gamma 結構的描述**——哪些履約價的未平倉夠大、
到期夠近、gamma 夠集中，因而在標的接近時會有比較多的避險流量經過。
把它當成方向訊號之前，請自己先跑過檢定（樣本外、控制波動叢聚與到期節奏）。

## 2. 標的價：顯示用現貨、定價用遠期

**參考標的價 S** 用證交所的發行量加權股價指數收盤，所以 Gamma Flip 講出來直接是現貨價位，
跟看盤軟體、跟別人的圖都對得起來。它只負責兩件事：GEX 的 `S²` 尺度，以及情境曲線的橫軸。

**選擇權定價則完全不碰現貨。** 每個到期別由自己的結算價 put-call parity 反解遠期價：

```
F = K + (C − P) / DF        DF = e^(−rT)，預設 r = 0 故 DF = 1
```

對價平 ±3% 內每個「買賣權結算價都有」的履約價各算一個 `F_k`，取**中位數**。
中位數而非平均，是為了不被冷門履約價的模型價拉走。

**為什麼不用加權指數現貨：** 用現貨會讓 parity 破裂，同一履約價的買權與賣權
會反解出不同的隱含波動率，gamma 就跟著不一致。用該到期別自己的遠期則沒有這個問題。

情境曲線平移時，各到期別遠期依 `F_e(S) = F_e × (S / S₀)` 同比例移動，保持基差比例。

實測（2026/08/21）：最近到期 45,076 → 最遠期 45,920，單調遞增，符合持有成本結構；
最近到期別在價平 ±3% 內的 `F_k` 散布只有 ±20 點。
抓不到加權指數時會自動退回「未平倉夠大且 parity 散布夠小」的到期別遠期，並在頁面上標示。

## 3. 到期時間 T

臺指選擇權的最終結算價，是在**最後交易日之次一營業日**開盤後 15 分鐘內決定的，
所以 T 要算到結算日，不是最後交易日。交易日數用 252 折成年，休市日表在 `calendar_tw.txt`。

**SPX 要分成兩種結算，不能混在一起算。** 根碼 `SPX` 是 **AM 結算**——每月第三個星期五
「開盤」用特別報價 SET 結算，最後交易日是前一個交易日（星期四）；根碼 `SPXW` 是 **PM 結算**
（週選 / 日選 / 月底選，當天收盤結算）。兩者在第三個星期五會落在同一個到期日，而且履約價
大量重疊——2026/08/21 那天共有 5 個日期重疊、2,672 檔契約撞在一起。解析時如果只用到期日
當 key，後讀到的那一批會把先讀到的整批蓋掉（實測最多會吃掉 40 萬口未平倉）。
本專案的做法是把 AM 那批另外用 `<到期日>A` 當 key，變成獨立的到期別。
**這不是可以忽略的細節：以未平倉量計，AM 結算佔 SPX 的 77%（16.9M / 22.0M 口）。**
AM 那批的 T 只算到最後交易日收盤，沒有把「收盤到隔天開盤」那段隔夜變異算進去，會略為低估。

**T 的慣例對 GEX 幾乎沒有影響。** 因為隱含波動率是從市價反解的，`σ√T` 被市價釘住；
而 `gamma ≈ φ(d₁)/(F·σ√T)`，分母裡的 `σ√T` 就是被釘住的那一項，T 的誤差自己抵消掉了。
**VEX 則會受影響**，因為 `vega = F·φ(d₁)·√T` 確實隨 T 變動。所以休市日表漏一兩天，
GEX 不用擔心，VEX 會有輕微偏差。

## 4. 隱含波動率：只取價外那一側，而且不要用割線法

**取價外側：** `K < F` 用賣權、`K ≥ F` 用買權反解，同履約價的買賣權共用這個 IV。

深度價內的結算價，時間價值常常只有一兩點（例如 2026/08/20 的 43200 買權，
結算價 1640、內含值 1637.5，時間價值 2.5 點）。IV 對這兩點的誤差極度敏感，
拿它反解會得到毫無意義的數字。價外側的時間價值就是全部的權利金，穩定得多。

副作用是 `gamma_call ≡ gamma_put`，於是單一履約價的 GEX 變成
`gamma × (Call OI − Put OI)` 這個乾淨形式——這正是 parity 成立時本來就該有的樣子。

**不要用純割線法反解 IV。** 這是本專案實際踩過的坑：深度價內時 `f(σ) = BS(σ) − price`
在低波動區幾乎是水平的，割線步長會停滯，跑滿迭代後回傳區間中點，
解出 250% 以上的假波動率，而且這種 leg 的 vega 很大，會嚴重污染 VEX。
現在的做法是**二分法保證收斂 + 牛頓法收尾**（4,000 組隨機往返測試零失敗）。

## 5. 三個定義式，以及 VEX 為什麼用 vanna 不用 vega

```
GEX(K) = M × S² × 0.01 × Σ sign × gamma × OI     元 / 標的移動 1%
VEX(K) = −M × S ÷ 100  × Σ sign × vanna × OI     元 / 隱含波動率 1 個百分點
GEX+   = GEX + β × VEX
M = 50 元/點（TXO 契約乘數）
```

GEX 的意義是「標的每移動 1%，造市商 delta 名目金額的變動量」——也就是他必須去市場上對沖掉的量。
VEX 是同一件事換一個驅動因子：「隱含波動率每上升 1 個百分點，造市商 delta 名目金額的變動量」。

**為什麼是 vanna：** 這張圖描述的是造市商**被迫調整的避險流量**。
vega 講的是部位損益（波動率動了賺賠多少），vanna（= ∂delta/∂sigma）講的才是流量
（波動率動了必須去市場上買賣多少標的）——跟 gamma 是同一類的東西，放在同一張地圖上才有一致的解讀。

還有一個現實理由。同履約價的買賣權 vega **完全相等**（put-call parity 的直接結果），
所以 `Σ sign × vega × OI` 在買權多賣權空的慣例下會退化成 `vega × (Call OI − Put OI)`，
多空幾乎完全抵消、恆在零附近（實測 2026/08/21 只有 +0.06 億），沒有資訊。
vanna 不會，因為它在價平兩側變號，跟 `Call OI − Put OI` 的變號剛好互相配合，
於是兩側同號、價平附近趨近於零——這就是 VEX 圖中間那個凹陷的來源，也是自我測試裡的一項不變量。

vega 曝險本身仍然有意義（造市商是波動率的多方還是空方），保留在摘要與資料表裡當附加欄位。

**GEX+ 的 β：** 直接解釋為「標的每移動 1%，隱含波動率反向變動 β 個波動點」。
β = 0 時 GEX+ 退化成 GEX（自我測試會檢查 flip 兩者完全相同）；β = 1 是常用的預設。
方向是股價指數的槓桿效應：現貨漲、隱含波動率跌。

## 6. 情境曲線與 Flip

把標的平移到 S，各到期別遠期同比例移動，隱含波動率固定在今天反解的值（**黏性履約價**假設），
重算全簿的 gamma 與 vanna。`Gamma Flip` 是 GEX 曲線最靠近現價的零交叉點（線性內插），
`GEX+ Flip` 是 `GEX + β×VEX` 曲線的零交叉點。

因為 VEX 在現價附近是負的，GEX+ Flip 通常會落在 Gamma Flip 的**上方**——
也就是把波動率回饋算進去之後，「進入負 gamma 區」的門檻其實比純 gamma 看到的更靠近現價。

## 7. 資料口徑

- 來源：期交所 `optDataDown`（TXO）與 `futDataDown`（TX），Big5 編碼 CSV
- 只取**一般交易時段**
- 用**結算價**不用收盤價：大量履約價當天沒有成交，收盤價是空的；結算價每個履約價都有
- 到期別碼：純六碼 = 月選、`W` = 週三選、`F` = 週五選
- 只保留最後交易日 ≥ 當日的到期別

## 8. 已知限制（還沒做的事）

1. **盤中不即時。** 期交所的未沖銷契約數是盤後才公布，這張圖描述的是**前一個收盤**
   的部位結構，不是盤中即時曝險。
2. **沒有流動性加權。** 冷門履約價的結算價是期交所用模型算的，目前權重跟熱門履約價一樣。
3. **情境曲線假設 IV 不隨標的變動**（β 只補了偏斜那一階），真實的 vol 反應更複雜。
4. **只做 TXO。** 沒有納入電子、金融、小台選擇權。
5. **輸出範圍截斷在 `S ± 25%`。** 帶外的分量另外存在 `truncated` 欄位裡，
   總量有加回去，介面上也有標示還有幾個履約價、多少口在範圍外。
   （`truncated` 只記了 gamma 與 vanna 兩組分量，vega 沒有另外拆出來；
   `totals.vc / vp` 本來就是全量，介面顯示的總 vega 曝險不受影響。）
6. **美股的調整後（非標準）序列沒有另外處理。** 例如 2026/08 的 QQQ 有一組
   `xxx.78` 的調整履約價（499.78、504.78 …… 609.78 共 23 個），來自過去的公司行動。
   這種序列的交割標的數量可能不是 100 股，套用 `multiplier = 100` 嚴格說並不精確。
   目前它們佔 |GEX| 約 0.09%、佔未平倉約 0.4%，先照標準合約處理。
   **介面上的「履約價分桶」已經會避開它們**：原生間距取的是「最小的、夠常見（≥15%）、
   而且能整除主間距」的那個間距，所以 0.22 不會被當成 QQQ 的履約價間距。


---

## 9. 與外部實作的交叉驗證

2026/08/21，拿本專案的輸出跟 GoOptions「大盤選擇權結構暴險地圖」同日的公布數字逐項對照
（雙方都以期交所公開資料為唯一輸入，實作各自獨立）：

| 項目 | 本專案 | 對照 | 差異 |
|---|---|---|---|
| 現貨 S | 45,224 | 45,224 | 0 |
| 到期日（9 個） | 08-26 / 08-28 / 09-02 / 09-04 / 09-16 / 10-21 / 11-18 / 12-16 / 03-17 | 完全相同 | — |
| 總 GEX | +11.36 億 | +11.50 億 | −1.2% |
| Gamma Flip | 44,706 | 44,695 | +11 點 |
| GEX+ Flip（β=1） | 45,008 | 44,985 | +23 點 |
| 總 VEX | −6.56 億 | −6.30 億 | +4.1% |
| 正 GEX 前 5 | 46,000 +2.96 / 46,500 +1.18 / 46,400 +1.12 / 45,800 +1.04 / 46,200 +1.04 | 46,000 +2.97 / 46,500 +1.18 / 46,400 +1.12 / 45,800 +1.05 / 46,200 +1.05 | ±0.01 |
| 負 GEX 前 5 | 44,000 −2.11 / 43,000 −1.34 / 44,500 −0.79 / 43,800 −0.62 / 40,000 −0.50 | 44,000 −2.10 / 43,000 −1.34 / 44,500 −0.78 / 43,800 −0.61 / 40,000 −0.50 | ±0.01 |
| 負 VEX 前 5 | 50,000 / 46,000 / 48,000 / 43,000 / 44,000 | 履約價與順序完全相同 | 量級高 3~11% |

GEX 這一側可以視為完全吻合。VEX 還有 3~11% 的系統性差距，最可能的來源是到期時間 T 的細節慣例：
vanna 有一個顯式的 `1/sigma`，而 sigma 是從市價反解的，T 一變 sigma 就跟著變，
所以 vanna 比 gamma 對 T 敏感（gamma 的 `sigma√T` 會自己抵消，見第 3 節）。
對照方沒有公開他們的 T 慣例，這個差距目前無法再收斂，如實記在這裡。
